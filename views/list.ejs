<%- include("header.ejs") -%>
  <div class="box" id="heading">
    <h1><%= todayIs %></h1>
  </div>

  <div class="box">
    <% Items.forEach(function(anItem){ %>

      <form action="/delete" method="post">
      <div class="item">
        <input type="checkbox" name="checkbox" value="<%= anItem._id %>" onChange="this.form.submit()">
        <p><%= anItem.name %></p>
      </div>
      </form>

    <% }); %>

    <form class="item" action="/" method="post">
      <input type="text" name="newItem" autocomplete="off">
      <button type="sumbit" name="submit button">+</button>
    </form>
  </div>
<%- include("footer.ejs") -%>
